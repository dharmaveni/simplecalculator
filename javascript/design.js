var box=document.getElementByIDd('display');

function addtoscreen(x);
    {
        box.value +=x;
		if(x=='Del')
		{
		box.value='';
	}
}
	function answer()
	{
		box.value=x;
		x=eval(x);
		box.value=x;
	}